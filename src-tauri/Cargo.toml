[package]
name = "streamscope"
version = "0.1.2"
description = "StreamScope — the streaming client developers actually enjoy using"
authors = ["you"]
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[lib]
# The `_lib` suffix may seem redundant but it is necessary
# to make the lib name unique and wouldn't conflict with the bin name.
# This seems to be only an issue on Windows, see https://github.com/rust-lang/cargo/issues/8519
name = "streamscope_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = [] }
tauri-plugin-opener = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"

# Async runtime
tokio = { version = "1", features = ["full"] }
async-trait = "0.1"

# Kafka client. `cmake-build` compiles librdkafka from source so the binary is
# self-contained and portable across macOS / Windows / Linux CI (requires cmake,
# which is preinstalled on GitHub runners). For fast local dev on macOS you can
# instead use features = ["dynamic-linking"] with `brew install librdkafka`.
rdkafka = { version = "0.36", features = ["cmake-build", "ssl-vendored"] }

# Error handling
thiserror = "1"

# UUID for connection ids
uuid = { version = "1", features = ["v4", "serde"] }

# Schema Registry: HTTP client to fetch schemas, and Avro decoding.
reqwest = { version = "0.12", features = ["json", "blocking", "rustls-tls"], default-features = false }
apache-avro = "0.17"
